# This file is controlled by Chef.  Changes will be overwritten.
# Regular cron jobs for the cron-apt package
#
# QA, Beta, and Overhead run Monday morning
# Production runs Thursday morning
<%= '0 10 * * 1	root	test -x /usr/sbin/cron-apt && /usr/sbin/cron-apt && /usr/local/bin/reboot-reminder yes' if @qa %>
<%= '0 10 * * 1	root	test -x /usr/sbin/cron-apt && /usr/sbin/cron-apt && /usr/local/bin/reboot-reminder yes' if @beta %>
<%= '0 10 * * 1	root	test -x /usr/sbin/cron-apt && /usr/sbin/cron-apt && /usr/local/bin/reboot-reminder yes' if @overhead %>
<%= '0 10 * * 4	root	test -x /usr/sbin/cron-apt && /usr/sbin/cron-apt && /usr/local/bin/reboot-reminder no' if @production %>
